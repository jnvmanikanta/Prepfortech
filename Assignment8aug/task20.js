function checker(n){
    let flag = "";
    switch(( n%2 === 0) ? "even": "not even")
    {
    case "even": 
        console.log("Given number is even");
        break;
    default:
        console.log("Not even number");
    } 
    }
    
    checker(2);
    checker(3);
